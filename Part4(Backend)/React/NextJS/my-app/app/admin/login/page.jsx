import React from 'react'

const page = () => {
  return (
    <div>This is admin login</div>
  )
}

export default page

export async function generateMetadata(params) {
    return{
        title: 'admin login lmao'
    }
}